<UserControl x:Class="DebugReplicator.View.VistaListaArchivos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DebugReplicator.View"
             xmlns:viewmodels="clr-namespace:DebugReplicator.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewmodels:VistaListaArchivosViewModel/>
    </UserControl.DataContext>
    <Grid Background="{StaticResource color_fondo_body}">
        <StackPanel>
            <Label Content="Seleccionar archivos para indexar" HorizontalAlignment="Center" Margin="0,20,0,0" FontSize="20" FontWeight="Bold"/>
            <StackPanel Orientation="Vertical" Width="700">
                <StackPanel Orientation="Horizontal">
                    <Border BorderBrush="Black" BorderThickness="0.25">
                        <TextBlock Text=""                   Width="30"  TextAlignment="Center"  />
                    </Border>
                    <Border BorderBrush="Black" BorderThickness="0.25">
                        <TextBlock Text="Nombre"             Width="400" TextAlignment="Center" />
                    </Border>
                    <Border BorderBrush="Black" BorderThickness="0.25">
                        <TextBlock Text="Fecha Modificacion" Width="150" TextAlignment="Center" />
                    </Border>
                    <Border BorderBrush="Black" BorderThickness="0.25">
                        <TextBlock Text="Tamano"             Width="100" TextAlignment="Center" />
                    </Border>
                    <Border BorderBrush="Black" BorderThickness="0.25">
                        <TextBlock Text=""                   Width="20"  TextAlignment="Center" />
                    </Border>
                    
                </StackPanel>
                <ListBox ItemsSource="{Binding FileItems, UpdateSourceTrigger=PropertyChanged}"
                            HorizontalContentAlignment="Stretch" Margin="0,20,0,0"/>
                              
            </StackPanel>
            
        </StackPanel>
    </Grid>
</UserControl>
