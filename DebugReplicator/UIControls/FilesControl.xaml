<UserControl x:Class="DebugReplicator.UIControls.FilesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DebugReplicator.UIControls"
             xmlns:Converters="clr-namespace:DebugReplicator.Converters"
             mc:Ignorable="d" 
             Height="20" d:DesignWidth="800"
             KeyDown="UserControl_KeyDown">
    <UserControl.Resources>
        <Converters:FileSizeFormatConverter x:Key="FileSizeFormatConverter"/>
        <Converters:IconToImageConverter x:Key="IconToImageConverter"/>
    </UserControl.Resources>
    <Grid MouseDown="Grid_MouseDown">
        <StackPanel Orientation="Horizontal" Width="700" HorizontalAlignment="Left">
            
            <Image Source="{Binding Icon, Converter={StaticResource IconToImageConverter}}"               Width="30" />            
            <TextBlock Text="{Binding Name}"                                                              Width="400"/>            
            <!--                                                                                          
            <TextBlock Text="{Binding DateCreated}"                                                       Width="125"/>
            -->
            <TextBlock Text="{Binding DateModified}"                                                      Width="150"/>
            <!--<TextBlock Text="{Binding Type}"                                                          Width="70"/>-->
            <TextBlock Text="{Binding SizeBytes, Converter={StaticResource FileSizeFormatConverter}}"     Width="100"/>
            <TextBlock Text=""     Width="20"/>
            
        </StackPanel>
    </Grid>
</UserControl>
